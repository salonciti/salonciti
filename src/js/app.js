$(document).ready(function() {

  $("#main-slideshow").owlCarousel({
    navigation : false, // Show next and prev buttons
    autoPlay: true,
    stopOnHover: true,
    autoHeight: true,
    slideSpeed : 300,
    paginationSpeed : 400,
    singleItem:true
  });

});
